// Type of data we'll use in our list
/* Ex: a list of int
    a list of char
    a list of double
    a list of boolean
    a list of list */
//Here we'll have all the functions about Elem

typedef int Elem; //We name Elem (of Element) the type we use, can be char, float, etc

void printElem(Elem e){
  printf("%d\n", e);
}
